<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.perfectify.eulogio.clockio" >

    <uses-permission android:name="android.permission.GET_TASKS" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".clockService">
            <intent-filter>
                <action android:name="com.perfectify.eulogio.clockio.InterimActivity"/>
                <action android:name=".clockService .BIND" />
            </intent-filter>
        </service>

        <activity
            android:name=".ResultActivity"
            android:label="@string/title_activity_result" >
        </activity>
        <activity
            android:name=".InterimActivity"
            android:label="@string/title_activity_interim"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="com.perfectify.eulogio.clockio.clockService" />
            </intent-filter>
        </activity>
    </application>

</manifest>
